# SoundCapsule Storage - Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        Client Applications                       │
│                  (Web, Mobile, Desktop Apps)                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTPS + JWT Bearer Token
                            │
                    ┌───────▼──────────┐
                    │    Traefik       │
                    │ Reverse Proxy    │
                    └───────┬──────────┘
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
         ▼                  ▼                  ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   Auth Service   │ │Storage Service  │ │  Other Services │
│ (External JWKS)  │ │  (Port 4002)    │ │                 │
└─────────────────┘ └────────┬────────┘ └─────────────────┘
         ▲                   │
         │                   │
         │         ┌─────────┴─────────┐
         │         │                   │
         │         ▼                   ▼
         │  ┌──────────────┐   ┌──────────────┐
         │  │   MySQL 8.0   │   │  AWS S3 /    │
         │  │   Database    │   │ DO Spaces    │
         │  └──────────────┘   └──────────────┘
         │
         └─── JWKS verification (/.well-known/jwks.json)
```

## 🔄 Request Flow

### 1️⃣ Upload Flow

```
┌──────┐                                                      ┌──────┐
│Client│                                                      │  S3  │
└───┬──┘                                                      └───┬──┘
    │                                                             │
    │ 1. POST /v1/files/upload-url                                │
    │    + JWT Token                                              │
    ├──────────────────────────────────────────┐                 │
    │                                           │                 │
    │                                    ┌──────▼──────┐          │
    │                                    │   Storage   │          │
    │                                    │   Service   │          │
    │                                    └──────┬──────┘          │
    │                                           │                 │
    │                          2. Validate JWT  │                 │
    │                          (JWKS from Auth) │                 │
    │                                           │                 │
    │                          3. Generate      │                 │
    │                          UUID fileKey     │                 │
    │                                           │                 │
    │                          4. Create signed │                 │
    │                          S3 upload URL    ├─────────────────┤
    │                          (15 min expiry)  │   getSignedUrl  │
    │                                           │                 │
    │                          5. Log to        │                 │
    │                          FileAudit table  │                 │
    │                                           │                 │
    │ 6. Return uploadUrl + fileKey             │                 │
    │◄──────────────────────────────────────────┤                 │
    │                                                             │
    │ 7. PUT to uploadUrl (direct to S3)                          │
    │    + File data                                              │
    ├─────────────────────────────────────────────────────────────►
    │                                                             │
    │ 8. 200 OK                                                   │
    │◄────────────────────────────────────────────────────────────┤
    │                                                             │
```

### 2️⃣ Playback Flow

```
┌──────┐                                                      ┌──────┐
│Client│                                                      │  S3  │
└───┬──┘                                                      └───┬──┘
    │                                                             │
    │ 1. GET /v1/files/playback-url/:fileKey                      │
    │    + JWT Token                                              │
    │    + ?expiresHours=24                                       │
    ├──────────────────────────────────────────┐                 │
    │                                           │                 │
    │                                    ┌──────▼──────┐          │
    │                                    │   Storage   │          │
    │                                    │   Service   │          │
    │                                    └──────┬──────┘          │
    │                                           │                 │
    │                          2. Validate JWT  │                 │
    │                          (JWKS from Auth) │                 │
    │                                           │                 │
    │                          3. Check file    │                 │
    │                          exists in S3     ├─────────────────┤
    │                          (HeadObject)     │   HeadObject    │
    │                                           │                 │
    │                          4. Generate      │                 │
    │                          signed playback  ├─────────────────┤
    │                          URL (24h expiry) │   getSignedUrl  │
    │                                           │                 │
    │                          5. Log to        │                 │
    │                          FileAudit table  │                 │
    │                                           │                 │
    │ 6. Return playbackUrl                     │                 │
    │◄──────────────────────────────────────────┤                 │
    │                                                             │
    │ 7. GET playbackUrl (direct from S3)                         │
    ├─────────────────────────────────────────────────────────────►
    │                                                             │
    │ 8. Audio file stream                                        │
    │◄────────────────────────────────────────────────────────────┤
    │                                                             │
```

## 📦 Component Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                         AppModule                             │
└───────────┬───────────────────────────────────────────────────┘
            │
            ├─────► ConfigModule (Global)
            │       └─── Environment Variables
            │
            ├─────► PrismaModule (Global)
            │       └─── Database Connection Pool
            │
            ├─────► AuthModule
            │       ├─── JwtStrategy (Passport)
            │       ├─── JwtAuthGuard
            │       └─── CurrentUser Decorator
            │
            └─────► FilesModule
                    ├─── FilesController
                    │    ├─── POST /v1/files/upload-url
                    │    ├─── GET /v1/files/playback-url/:fileKey
                    │    └─── GET /v1/files/:fileKey/metadata
                    │
                    ├─── FilesService
                    │    ├─── createUploadUrl()
                    │    ├─── getPlaybackUrl()
                    │    └─── getFileMetadata()
                    │
                    └─── S3Service
                         ├─── generateUploadUrl()
                         ├─── generatePlaybackUrl()
                         └─── getFileMetadata()
```

## 🗄️ Data Model

```
┌─────────────────────────────────────────────────────┐
│                   FileAudit                         │
├─────────────────────────────────────────────────────┤
│ id              : String (UUID, PK)                 │
│ userId          : String (from JWT)                 │
│ fileKey         : String (UUID, S3 key)             │
│ action          : String (upload/playback)          │
│ fileName        : String? (original name)           │
│ contentType     : String? (MIME type)               │
│ durationSeconds : Int? (audio length)               │
│ metadata        : Json? (extra data)                │
│ createdAt       : DateTime (auto)                   │
├─────────────────────────────────────────────────────┤
│ Indexes:                                            │
│   - userId                                          │
│   - fileKey                                         │
│   - createdAt                                       │
└─────────────────────────────────────────────────────┘
```

## 🔐 Authentication Flow

```
1. Client sends request with JWT in Authorization header:
   Authorization: Bearer eyJhbGciOiJSUzI1Ni...

2. JwtAuthGuard intercepts request
   └─► Extract token from header
   └─► Decode token to get 'kid' (key ID)
   └─► Fetch public key from JWKS endpoint
       https://auth.soundcapsule.com/.well-known/jwks.json
   └─► Verify signature with public key
   └─► Validate claims (issuer, audience, expiry)
   └─► Extract user data from token payload

3. CurrentUser decorator provides user data:
   {
     userId: "user-123",
     email: "user@example.com",
     roles: ["user"]
   }

4. Controller method executes with authenticated user context
```

## 🌐 Network Topology (Docker)

```
┌──────────────────── web network ────────────────────────┐
│                                                          │
│  ┌──────────┐         ┌─────────────────────┐           │
│  │ Traefik  ├────────►│  Storage Service    │           │
│  │  :80     │         │   (soundcapsule-    │           │
│  │  :8080   │         │    storage)         │           │
│  └──────────┘         │   Port: 4002        │           │
│                       └──────────┬──────────┘           │
└──────────────────────────────────┼──────────────────────┘
                                   │
┌──────────────── soundcapsule network ───────────────────┐
│                                  │                       │
│                       ┌──────────▼──────────┐            │
│                       │  MySQL Database     │            │
│                       │  (soundcapsule-     │            │
│                       │   storage-db)       │            │
│                       │  Port: 3306         │            │
│                       └─────────────────────┘            │
│                                                          │
└──────────────────────────────────────────────────────────┘
                                   │
                                   │ (External)
                                   │
                       ┌───────────▼──────────┐
                       │   AWS S3 / DO Spaces │
                       │   (External Storage) │
                       └──────────────────────┘
```

## 🔄 Deployment Pipeline

```
┌──────────────┐
│ Source Code  │
│   (GitHub)   │
└──────┬───────┘
       │
       │ git push
       ▼
┌──────────────┐
│  CI/CD       │
│  Pipeline    │
└──────┬───────┘
       │
       │ npm install
       │ npm run build
       │ npm test
       │ docker build
       │
       ▼
┌──────────────┐
│ Docker Image │
│  Registry    │
└──────┬───────┘
       │
       │ docker pull
       │ docker-compose up
       │
       ▼
┌──────────────┐
│  Production  │
│   Server     │
└──────────────┘
```

## 📊 Monitoring & Observability

```
┌─────────────────────────────────────────────────────┐
│               Application Metrics                    │
├─────────────────────────────────────────────────────┤
│  • Request rate (requests/sec)                      │
│  • Response time (avg, p95, p99)                    │
│  • Error rate (4xx, 5xx)                            │
│  • Active connections                               │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│               Business Metrics                       │
├─────────────────────────────────────────────────────┤
│  • Upload requests/day                              │
│  • Playback requests/day                            │
│  • Unique users                                     │
│  • Storage usage                                    │
│  • Average file size                                │
│  • Duration distribution                            │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│               Infrastructure Metrics                 │
├─────────────────────────────────────────────────────┤
│  • CPU usage                                        │
│  • Memory usage                                     │
│  • Database connections                             │
│  • Database query performance                       │
│  • S3 API latency                                   │
└─────────────────────────────────────────────────────┘
```

## 🔒 Security Layers

```
┌─────────────────────────────────────────────────────┐
│  Layer 1: Network Security                          │
│  • HTTPS/TLS encryption                             │
│  • Traefik reverse proxy                            │
│  • Private Docker networks                          │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  Layer 2: Authentication                            │
│  • JWT token validation                             │
│  • JWKS signature verification                      │
│  • Token expiration checks                          │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  Layer 3: Authorization                             │
│  • User ID extraction from token                    │
│  • Audit logging of all actions                     │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  Layer 4: Input Validation                          │
│  • DTO validation (class-validator)                 │
│  • File name sanitization                           │
│  • Content type whitelist                           │
│  • Duration limits                                  │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  Layer 5: Data Protection                           │
│  • Signed URLs with expiration                      │
│  • No public S3 access                              │
│  • Environment variable secrets                     │
│  • SQL injection protection (Prisma)                │
└─────────────────────────────────────────────────────┘
```

## 🚀 Scaling Strategy

### Horizontal Scaling
```
                    ┌──────────────┐
                    │ Load Balancer│
                    └───────┬──────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│  Instance 1   │   │  Instance 2   │   │  Instance 3   │
│  (stateless)  │   │  (stateless)  │   │  (stateless)  │
└───────┬───────┘   └───────┬───────┘   └───────┬───────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                    ┌───────▼───────┐
                    │  MySQL        │
                    │  (Shared)     │
                    └───────────────┘
```

### Vertical Scaling
- Increase container resources (CPU, Memory)
- Optimize database queries and indexes
- Enable connection pooling (already done with Prisma)
- Cache JWKS responses

---

**Architecture Version:** 1.0.0  
**Last Updated:** December 8, 2025
